@using CDCPortal.Web.UIComponents.JsTree;

@model JsTreeLoadAjaxData

//If initial load
if(obj.id === "#"){
    callback.call(this, [@await Model.InitialData.InvokeAsync(Component)]);
} else{
    @await Model.Result.InvokeAsync(Component)

    if(result === false)
        return callback.call(this, []);

    var deserialized = eval(result);
    return callback.call(this, deserialized);
}